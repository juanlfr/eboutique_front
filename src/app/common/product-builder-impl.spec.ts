import { ProductBuilderImpl } from './product-builder-impl';

describe('ProductBuilderImpl', () => {
  it('should create an instance', () => {
    expect(new ProductBuilderImpl()).toBeTruthy();
  });
});
